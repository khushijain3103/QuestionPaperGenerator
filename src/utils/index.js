const { computeMarksBasedOnPercentage,
    convertToNumber,
    getQuestions } = require("../utils/question");

module.exports = {
    computeMarksBasedOnPercentage,
    convertToNumber,
    getQuestions
};
